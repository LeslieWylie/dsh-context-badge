// The companion has no host-side tools or network service. The DSH bundle only
// needs this entry so the official loader can mount the browser client half.
export default {
  inject: [],
  apply() {},
}
